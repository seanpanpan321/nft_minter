import { Heading, VStack } from '@chakra-ui/react';

const Home = () => {
  return (
    <VStack w={'full'}>
      <Heading size="md" marginBottom={6}>
        Weclome to DFPay Demo App
      </Heading>
    </VStack>
  );
};

export default Home;
